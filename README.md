# Emmy's Coding Game

A fun coding game that runs in the browser, hosted on GitHub Pages.

## Play

Open `index.html` in a browser, or visit the GitHub Pages URL once deployed.

## Development

No build step needed — edit the files and open `index.html` directly.

## Deploy

Push to `main` and enable GitHub Pages (Settings → Pages → Deploy from branch → `main` / `root`).
